# Intended theorem chain and scope

Every new Lean statement listed here is **source written, not compiled**. This ledger describes what the build must prove, not what this authoring session has already formally established.

| Declaration | Required meaning |
|---|---|
| `T3mix_eq_row_hybrid` | Literal accepted table equals the N/A row hybrid of the two pinned tables. |
| `S3mix` | All within-family and required cross-family separations hold. No global disjointness assumption. |
| `Rx27` / `stepx27` | Apply the new table only to the unchanged ordered children h7,p11,h9 and obtain all seven actual weights. |
| `terminal_value` | The unchanged terminal code on modified x27, original x29 and original b13 evaluates to the literal N1. |
| `code69` / `independent_code69` / `card_code69` | An explicitly defined finite set, independent in the 69th base-graph power, has cardinality N1 through `card_multiCodeSet`. |
| `code207` / `card_code207` / `independent_code207` | The same set, transported through pinned isomorphisms, lies in the actual 207th strong power of Mathlib's cycle graph. |
| `exists_code207` | Unconditional existence of an independent set of exactly N1 words in the stated graph and dimension. |
| `alpha_207_ge` | Independence-number inequality obtained from that same constructed set. |
| `shannonCapacity_cycleGraph_11_ge_root` | Root lower bound derived from `alpha_207_ge`, not a separate assumption. |
| `shannonCapacity_cycleGraph_11_ge` | The exact reported decimal is a lower bound on Shannon capacity. |
| `strictly_larger` / `exact_capacity_comparison` / `candidate_root_strictly_improves` | Comparison to the full pinned 150-digit baseline, not merely its truncated display. |

The final graph is `SimpleGraph.cycleGraph 11` and its 207th strong power. Physical terminal dimensions are 81,87,39; the corresponding base-block counts are 27,29,13. The generic construction's base-weight hypothesis is discharged by the imported actual base system before the final theorem.

The second decimal endpoint is represented only as `candidate_root_upper_integer`. It bounds the root of this constructed set, never Shannon capacity from above.

The audit file gives independent type annotations for the exact set, decimal capacity claim, and root comparison, including both full integers. This is intended to catch accidental hypotheses, altered dimensions, graph substitutions, or disconnected arithmetic statements during elaboration.

Scope exclusions: no optimality claim, no unrestricted two-sided extension, no priority clearance, no VibeMathed score guarantee, and no claim that an unbuilt file proves a theorem.
